# Application Name
spring.application.name=University Management System

# Server Configuration
server.port=8080

# PostgreSQL Database Configuration (Railway)
# Use Railway DATABASE_URL or configure individual properties
spring.datasource.url=jdbc:postgresql://postgres.railway.internal:5432/railway
spring.datasource.username=postgres
spring.datasource.password=yMyLJbpsZnERBgzjyCGorMlUlyKDZDEJ

# JPA/Hibernate Configuration
# CRITICAL: DO NOT generate schema - database already exists on Railway
spring.jpa.hibernate.ddl-auto=none
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Thymeleaf Configuration
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.uniflow=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# File Upload (if needed later)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
